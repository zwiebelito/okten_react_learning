import React from 'react';
import {MovieInfo} from "../../../components";

const MovieInfoPage = () => {
    return (
        <div>
            <MovieInfo />
        </div>
    );
};

export {MovieInfoPage};